apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bizscraper-sqlite-data
  namespace: mvpoc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 1Gi
